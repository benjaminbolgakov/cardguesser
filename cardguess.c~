
#include <stdio.h>
#include <string.h>
#include "deck.h"

void prompt(struct deck* d);





int main()
{
    struct deck d;
    prompt(&d);
    print_deck(&d);
    return 0;
}


void prompt(struct deck* d){
    printf("Pick a card and remember it! \n");
    //print_deck(d);
    printf("Ready? \n");
}
